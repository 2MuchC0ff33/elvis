# README

## Elvis: Australian Sales Lead Call List Scraper

### Design

Data-Driven Programming Workflow (ASCII Art Representation)

+-------------------+     +-------------------+     +-------------------+
|   Data Input      | --> | Pattern Matching  | --> | Action Execution  |
| (Streams: logs,   |     | (Regex, line nums)|     | (Filter, transform|
|  CSV, emails)     |     |                   |     |  aggregate, call  |
+-------------------+     +-------------------+     |  programs)        |
                              |                     +-------------------+
                              | (No match)                |
                              v                           |
                       +-------------------+              |
                       | Default Action    | <----------+
                       | (Print line,      |
                       |  deliver msg,     |
                       |  error)           |
                       +-------------------+

Stages Overview:

1. **Data Input**: Receive structured data streams using `sh`. Using commands like `curl` (fetch data from URLs), `cat` (read/concatenate files), `dd` (convert/copy streams), or `tee` (duplicate input).
2. **Pattern Matching**: Match conditions with tools like `awk` (pattern scanning/processing), `sed` (stream editing/matching), or `grep` (search patterns).
3. **Action Execution**: Perform processing via `sh` (execute shell scripts), `xargs` (invoke utilities with arguments), or file operations like `cp`, `mv`, `rm`.
4. **Default Action**: Handle unmatched cases using `sh` (fallback scripts), `sed` (default print), or `echo` (output messages).
5. **Loop**: Repeat for each input element with `awk`'s built-in line-by-line loop, `sh` (while/for loops), or `xargs` (iterative processing).

### Sources

- [Unix Filesystem Layout](https://en.wikipedia.org/wiki/Unix_filesystem#Conventional_directory_layout)
- [RC File](http://www.catb.org/jargon/html/R/rc-file.html)
- [GitHub Flavored Markdown](https://github.github.com/gfm/)
- [Pseudocode Standard - Program Design Language](https://users.csc.calpoly.edu/~jdalbey/SWE/pdl_std.html)
- [Mermaid Syntax Reference](https://mermaid.ai/open-source/intro/syntax-reference.html)
- [List of POSIX commands](https://en.wikipedia.org/wiki/List_of_POSIX_commands)
- [Sed](https://en.wikipedia.org/wiki/Sed)
- [Awk](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/awk.html)
